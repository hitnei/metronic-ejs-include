const BaseView = require('../../../../viewController/baseView');

class notifications extends BaseView {
    constructor() {
      super()
      this.fileName = 'notifications'
    }
    getFileName() {
      return this.fileName;
    }
    getDefaultVariables() {
      // No aguments are require. Return sample data
      let defaultVariables = {}
      
      try {
        defaultVariables = require('./notificationsData.json');
      } catch (error) {
        console.log(error);
  
        return {}
      }
  
      return defaultVariables;
    }
  }
  module.exports = notifications;